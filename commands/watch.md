---
description: Watch a video from a URL or local file and answer questions about it.
argument-hint: "<video-url-or-path> [question]"
allowed-tools: Bash, Read, Edit, AskUserQuestion
---

Delegate to the `watch` skill in `SKILL.md` using `$ARGUMENTS`. If no source is
provided, ask the user for a video URL or local path before running the runtime.
